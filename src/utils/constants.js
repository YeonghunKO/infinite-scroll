const LOADER_SHOW_CLASS = 'show';

const RENDER_TYPE = {
  ADD_POSTS: 'addPosts',
  FILTER: 'filter',
};

const POST_SHOW_CLASS = 'invisible';

export { LOADER_SHOW_CLASS, RENDER_TYPE, POST_SHOW_CLASS };
